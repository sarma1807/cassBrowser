# Cassandra Browser — To-Do List

---

##### from Asma :

- Query Box : Needs to be larger or expandable
- Data Copying : Make query result rows copyable (e.g., pulling a single column value from a row)
- Also, a right-click "Copy as CSV" or "Copy as INSERT statement" would be huge, which I use from DataStax
- Row Limits : Is there a reason it's hard-capped at a max limit of 200 ?
- Editor/File Management : Would love support for multiple saved CQL query tabs/windows
- for queries : standard file options (File -> Open from file, Save query, etc.)

---

